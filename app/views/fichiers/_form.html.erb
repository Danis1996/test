<%= form_with(model: fichier, local: true) do |form| %>
  <% if fichier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fichier.errors.count, "error") %> prohibited this fichier from being saved:</h2>

      <ul>
        <% fichier.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titre %>
    <%= form.text_field :titre %>
  </div>

  <div class="field">
    <%= form.label :contenu %>
    <%= form.text_field :contenu %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
