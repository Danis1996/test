<%= form_with(model: fichier_reclamation, local: true) do |form| %>
  <% if fichier_reclamation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fichier_reclamation.errors.count, "error") %> prohibited this fichier_reclamation from being saved:</h2>

      <ul>
        <% fichier_reclamation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reclamation_id %>
    <%= form.text_field :reclamation_id %>
  </div>

  <div class="field">
    <%= form.label :fichier_id %>
    <%= form.text_field :fichier_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
